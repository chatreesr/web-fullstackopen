{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/phonebook",
    "deploy:full": "npm run build:ui && cd ../../ && git add . && git commit -m uibuild && git push origin main",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chatree Sridaoduan",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
